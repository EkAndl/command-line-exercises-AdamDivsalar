mkdir -p ~/cli_sandbox
cd ~/cli_sandbox

1: Create a directory "recommend" and then delete it.
mkdir recommend && rmdir recommend

2: Create two directories "medal" and "terminal" and move into "terminal".
mkdir medal terminal && cd terminal

3: Create a file "evaluate.txt" containing: "rain evaluate recommend evaluate discover". Then search recursively for "evaluate".
echo "rain evaluate recommend evaluate discover" > evaluate.txt && grep -r "evaluate" .

4: Output who the active user is.
whoami

5: Change directory to "cli_sandbox".
cd ~/cli_sandbox

6: Change directory to "cli_sandbox".
cd ~/cli_sandbox

7: Create a directory "needle" and then delete it.
mkdir needle && rmdir needle

8: Delete the last folder you created.
rmdir terminal

9: Write something in a file called "bay.txt".
echo "something" > bay.txt

10: Change directory to "cli_sandbox".
cd ~/cli_sandbox

11: Create two directories "regret" and "plane" and move into "plane".
mkdir regret plane && cd plane

12: Change directory to "cli_sandbox".
cd ~/cli_sandbox

13: Delete the last file you created.
rm bay.txt

14: Open a file in the terminal and see the content.
cat on.txt

15: Output hidden files.
ls -la

16: Output who the active user is.
whoami

17: Open a file in the terminal and see the content.
cat plane.txt

18: Delete the last file you created.
rm plane.txt

19: Create a directory "surprise" and then delete it.
mkdir surprise && rmdir surprise

20: Output what's in the directory.
ls

21: Open a file in the terminal and see the content.
cat on.txt

22: Create a directory "regret" and then delete it.
mkdir regret && rmdir regret

23: Output hidden files.
ls -la

24: Open a file in the terminal and see the content.
cat on.txt

25: Open a file in the terminal and see the content.
cat on.txt

26: Open a file in the terminal and see the content.
cat on.txt

27: Create a directory "shot" and then delete it.
mkdir shot && rmdir shot

28: Delete the last folder you created.
rmdir plane

29: Use: echo "appreciate evaluate banish sister sister" > "on.txt" to create or overwrite "on.txt".
echo "appreciate evaluate banish sister sister" > "on.txt"

30: Create a file "plane.txt" containing: "appreciate support plane rain surprise". Then search recursively for "plane".
echo "appreciate support plane rain surprise" > plane.txt && grep -r "plane" .

31: Output the full path you are on.
pwd

32: Create a directory "appreciate" and then delete it.
mkdir appreciate && rmdir appreciate

33: Create a file "correspondence.txt" containing: "recommend advantage recommend excuse recommend". Then output the sorted lines.
echo "recommend advantage recommend excuse recommend" > correspondence.txt && sort correspondence.txt

34: Output hidden files.
ls -la

35: Output what's in the directory.
ls

36: Delete the last folder you created.
rmdir medal

37: Delete the last file you created.
rm correspondence.txt

38: Create two directories "experienced" and "sister" and move into "sister".
mkdir experienced sister && cd sister

39: Create a folder named "cli_sandbox".
mkdir cli_sandbox

40: Create a file called "recession.txt".
touch recession.txt

41: Create a directory "shot" and then delete it.
mkdir shot && rmdir shot

42: Use: echo "recommend excuse stock rain rider" > "needle.txt" to create or overwrite "needle.txt".
echo "recommend excuse stock rain rider" > "needle.txt"

43: Write something in a file called "degree.txt".
echo "something" > degree.txt

44: Output the full path you are on.
pwd

45: Create a file "excuse.txt" containing: "advantage difficulty advantage honor advantage". Then output the sorted lines.
echo "advantage difficulty advantage honor advantage" > excuse.txt && sort excuse.txt

46: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

47: If already inside "cli_sandbox", create a directory "ditch" and move into the directory.
cd ~/cli_sandbox && mkdir ditch && cd ditch

48: Create a file "honor.txt" containing: "shot appreciate shot honor shot". Then output the unique lines.
echo "shot appreciate shot honor shot" > honor.txt && echo "shot appreciate shot honor shot" | tr ' ' '\n' | sort -u

49: Output the full path you are on.
pwd

50: Output the full path you are on.
pwd

51: If there are any directories in the current folder, then move inside any one of them.
cd ~/cli_sandbox && cd $(ls -d */ | head -1)

52: Delete the last folder you created.
cd ~/cli_sandbox && rmdir ditch

53: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

54: Use: echo "relative relative medal panel rain" > "shot.txt" to create or overwrite "shot.txt".
echo "relative relative medal panel rain" > "shot.txt"

55: Create a file "degree.txt" containing: "architecture advantage architecture recommend architecture". Then output the unique lines.
echo "architecture advantage architecture recommend architecture" > degree.txt && echo "architecture advantage architecture recommend architecture" | tr ' ' '\n' | sort -u

56: Output who the active user is.
whoami

57: Create a file "pray.txt" containing: "beat honor plane appreciate pray". Then search recursively for "pray".
echo "beat honor plane appreciate pray" > pray.txt && grep -r "pray" .

58: Create a file called "speculate.txt".
touch speculate.txt

59: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

60: Use: echo "nerve nerve honor speculate nerve" > "experienced.txt" to create or overwrite "experienced.txt".
echo "nerve nerve honor speculate nerve" > "experienced.txt"

61: Output the full path you are on.
pwd

62: Create two directories "sister" and "stock" and move into "stock".
mkdir sister stock && cd stock

63: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

64: Open a file in the terminal and see the content.
cat pray.txt

65: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

66: Change directory to "cli_sandbox".
cd ~/cli_sandbox

67: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

68: Output the sizes of the files in the directory.
ls -lh

69: Output what's in the directory.
ls

70: Delete the last file you created.
cd ~/cli_sandbox && rm experienced.txt

71: Output the full path you are on.
pwd

72: Create a file "bay.txt" containing: "correspondence bay experienced evaluate evaluate". Then search recursively for "bay".
echo "correspondence bay experienced evaluate evaluate" > bay.txt && grep -r "bay" .

73: Output hidden files.
ls -la

74: Open a file in the terminal and see the content.
cat bay.txt

75: Output what's in the directory.
ls

76: Output the sizes of the files in the directory.
ls -lh

77: Use: echo "cater banish ethics shot experienced" > "appreciate.txt" to create or overwrite "appreciate.txt".
echo "cater banish ethics shot experienced" > "appreciate.txt"

78: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

79: Output who the active user is.
whoami

80: Delete the last folder you created.
rmdir stock

81: Output who the active user is.
whoami

82: Output the full path you are on.
pwd

83: Output what's in the directory.
ls

84: Create two directories "plane" and "degree" and move into "degree".
mkdir plane degree && cd degree

85: Create a file called "recommend.txt" and immediately delete it.
touch recommend.txt && rm recommend.txt

86: If already inside "cli_sandbox", create a directory "discover" and move into the directory.
cd ~/cli_sandbox && mkdir discover && cd discover

87: If there are any directories in the current folder, then move inside any one of them.
cd ~/cli_sandbox && cd $(ls -d */ | head -1)

88: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

89: Open a file in the terminal and see the content.
cat appreciate.txt

90: Create a file called "rider.txt" and immediately delete it.
touch rider.txt && rm rider.txt

91: Output the full path you are on.
pwd

92: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

93: Output the full path you are on.
pwd

94: Delete the last file you created.
rm appreciate.txt

95: Write something in a file called "advantage.txt".
echo "something" > advantage.txt

96: Change directory to "cli_sandbox".
cd ~/cli_sandbox

97: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

98: Create a file called "architecture.txt" containing these words (one per line): "on appreciate on difficulty on". Then count the lines.
echo -e "on\nappreciate\non\ndifficulty\non" > architecture.txt && wc -l architecture.txt

99: Create a file "recession.txt" containing: "on cater on needle on". Then output the sorted lines.
echo "on cater on needle on" > recession.txt && echo "on cater on needle on" | tr ' ' '\n' | sort

100: Create a folder named "cli_sandbox".
cd ~/cli_sandbox && mkdir -p cli_sandbox

101: Create a directory "correspondence" and then delete it.
mkdir correspondence && rmdir correspondence

102: Output hidden files.
ls -la

103: Output the full path you are on.
pwd

104: Create a file called "regret.txt".
touch regret.txt

105: Create a directory "shot" and then delete it.
mkdir shot && rmdir shot

106: Open a file in the terminal and see the content.
cat advantage.txt

107: Create two directories "banish" and "recession" and move into "recession".
mkdir banish recession && cd recession

108: Create a file called "hill.txt".
touch hill.txt

109: Open a file in the terminal and see the content.
cd ~/cli_sandbox && cat bay.txt

110: Create a file "beat.txt" containing: "pray rain correspondence beat sister". Then search recursively for "beat".
echo "pray rain correspondence beat sister" > beat.txt && grep -r "beat" .

111: Output what's in the directory.
ls

112: Delete the last file you created.
rm beat.txt

113: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

114: Create a directory "evaluate" and then delete it.
mkdir evaluate && rmdir evaluate

115: Write something in a file called "experienced.txt".
echo "something" > experienced.txt

116: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

117: If already inside "cli_sandbox", create a directory "stock" and move into the directory.
mkdir stock && cd stock

118: Output what's in the directory.
ls

119: Delete the last folder you created.
cd ~/cli_sandbox && rmdir stock

120: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

121: Output the content of any file in the directory.
cat advantage.txt

122: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

123: Output hidden files.
ls -la

124: Open a file in the terminal and see the content.
cd ~/cli_sandbox && cat bay.txt

125: Output the full path you are on.
pwd

126: Delete the last folder you created.
rm -rf recession

127: Create two directories "ethics" and "pioneer" and move into "pioneer".
mkdir ethics pioneer && cd pioneer

128: Delete the last file you created.
cd ~/cli_sandbox && rm experienced.txt

129: Create a directory "plane" and then delete it.
mkdir plane && rmdir plane

130: Output what's in the directory.
ls

131: Create a file called "interference.txt" and immediately delete it.
touch interference.txt && rm interference.txt

132: Open a file in the terminal and see the content.
cat advantage.txt

133: Output what's in the directory.
ls

134: Create a file called "correspondence.txt" and immediately delete it.
touch correspondence.txt && rm correspondence.txt

135: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

136: Delete the last folder you created.
rmdir pioneer

137: Use: echo "medal identity depression degree terminal" > "correspondence.txt" to create or overwrite "correspondence.txt".
echo "medal identity depression degree terminal" > "correspondence.txt"

138: Change directory to "cli_sandbox".
cd ~/cli_sandbox

139: Write something in a file called "pen.txt".
echo "something" > pen.txt

140: Use: echo "appreciate beat ethics correspondence proportion" > "architecture.txt" to create or overwrite "architecture.txt".
echo "appreciate beat ethics correspondence proportion" > "architecture.txt"

141: Output the sizes of the files in the directory.
ls -lh

142: Create a file "speculate.txt" containing: "speculate support honor salvation rider". Then search recursively for "speculate".
echo "speculate support honor salvation rider" > speculate.txt && grep -r "speculate" .

143: Output what's in the directory.
ls

144: Output the content of any file in the directory.
cat pen.txt

145: Use: echo "pray advice regret recommend shallow" > "proportion.txt" to create or overwrite "proportion.txt".
echo "pray advice regret recommend shallow" > "proportion.txt"

146: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

147: Create a file "proportion.txt" containing: "ditch banish ditch appreciate ditch". Then output the sorted lines.
echo "ditch banish ditch appreciate ditch" > proportion.txt && echo "ditch banish ditch appreciate ditch" | tr ' ' '\n' | sort

148: Output who the active user is.
whoami

149: Change directory to "cli_sandbox".
cd ~/cli_sandbox

150: Create a file called "ethics.txt" containing these words (one per line): "banish rain banish honor banish". Then count the lines.
echo -e "banish\nrain\nbanish\nhonor\nbanish" > ethics.txt && wc -l ethics.txt

151: Open a file in the terminal and see the content.
cat ethics.txt

152: Output what's in the directory.
ls

153: Output what's in the directory.
ls

154: Output who the active user is.
whoami

155: Open a file in the terminal and see the content.
cat architecture.txt

156: Use: echo "terminal cater rain interference shot" > "beat.txt" to create or overwrite "beat.txt".
echo "terminal cater rain interference shot" > "beat.txt"

157: Use: echo "correspondence shot ethics experienced advantage" > "ditch.txt" to create or overwrite "ditch.txt".
echo "correspondence shot ethics experienced advantage" > "ditch.txt"

158: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

159: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

160: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

161: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

162: Output hidden files.
ls -la

163: Create a file "plane.txt" containing: "plane advantage ditch salvation medal". Then search recursively for "plane".
echo "plane advantage ditch salvation medal" > plane.txt && grep -r "plane" .

164: Output the full path you are on.
pwd

165: Create a file "experienced.txt" containing: "excuse banish excuse appreciate excuse". Then output the unique lines.
echo "excuse banish excuse appreciate excuse" > experienced.txt && echo "excuse banish excuse appreciate excuse" | tr ' ' '\n' | sort -u

166: Create a file called "support.txt".
touch support.txt

167: Create a file called "beat.txt" containing these words (one per line): "plane regret plane rain plane". Then count the lines.
echo -e "plane\nregret\nplane\nrain\nplane" > beat.txt && wc -l beat.txt

168: Create a file called "rain.txt".
touch rain.txt

169: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

170: If already inside "cli_sandbox", create a directory "ethics" and move into the directory.
mkdir ethics && cd ethics

171: Output hidden files.
ls -la

172: Write something in a file called "interference.txt".
echo "something" > interference.txt

173: If already inside "cli_sandbox", create a directory "honor" and move into the directory.
cd ~/cli_sandbox && mkdir honor && cd honor

174: Create a directory "plane" and then delete it.
mkdir plane && rmdir plane

175: Create a folder named "cli_sandbox".
mkdir cli_sandbox

176: Create a file called "relative.txt" and immediately delete it.
touch relative.txt && rm relative.txt

177: Create a file called "regret.txt" containing these words (one per line): "stock cousin stock difficulty stock". Then count the lines.
echo -e "stock\ncousin\nstock\ndifficulty\nstock" > regret.txt && wc -l regret.txt

178: Output what's in the directory.
ls

179: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

180: Output who the active user is.
whoami

181: Delete the last folder you created.
cd ~/cli_sandbox && rm -rf honor

182: Output who the active user is.
whoami

183: Use: echo "rider ethics cater salvation rain" > "interference.txt" to create or overwrite "interference.txt".
echo "rider ethics cater salvation rain" > "interference.txt"

184: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

185: Create a directory "banish" and then delete it.
mkdir banish && rmdir banish

186: Delete the last file you created.
cd ~/cli_sandbox && rm interference.txt

187: Create two directories "ethics" and "interference" and move into "interference".
mkdir ethics interference && cd interference

188: If there are any directories in the current folder, then move inside any one of them.
cd ~/cli_sandbox && cd $(ls -d */ | head -1)

189: Output the sizes of the files in the directory.
ls -lh

190: Create a file "surprise.txt" containing: "bay architecture shallow surprise on". Then search recursively for "surprise".
cd ~/cli_sandbox && echo "bay architecture shallow surprise on" > surprise.txt && grep -r "surprise" .

191: Delete the last folder you created.
rmdir interference

192: Output the full path you are on.
pwd

193: Create a file called "hill.txt" containing these words (one per line): "depression proportion depression responsibility depression". Then count the lines.
echo -e "depression\nproportion\ndepression\nresponsibility\ndepression" > hill.txt && wc -l hill.txt

194: Create a file called "sister.txt" containing these words (one per line): "cousin discover cousin correspondence cousin". Then count the lines.
echo -e "cousin\ndiscover\ncousin\ncorrespondence\ncousin" > sister.txt && wc -l sister.txt

195: Output hidden files.
ls -la

196: If already inside "cli_sandbox", create a directory "salvation" and move into the directory.
mkdir salvation && cd salvation

197: Create a file called "recession.txt" containing these words (one per line): "recommend sister recommend ethics recommend". Then count the lines.
echo -e "recommend\nsister\nrecommend\nethics\nrecommend" > recession.txt && wc -l recession.txt

198: Output the content of any file in the directory.
cat recession.txt

199: Create a file called "pioneer.txt" containing these words (one per line): "salvation rider salvation discover salvation". Then count the lines.
echo -e "salvation\nrider\nsalvation\ndiscover\nsalvation" > pioneer.txt && wc -l pioneer.txt

200: Output hidden files.
ls -la

201: Output the full path you are on.
pwd

202: If there are any directories in the current folder, then move inside any one of them.
cd ~/cli_sandbox && cd $(ls -d */ | head -1)

203: Output what's in the directory.
ls

204: Output hidden files.
ls -la

205: Output what's in the directory.
ls

206: Create a file called "advantage.txt" and immediately delete it.
touch advantage.txt && rm advantage.txt

207: Use: echo "interference architecture appreciate on recommend" > "recession.txt" to create or overwrite "recession.txt".
cd ~/cli_sandbox && echo "interference architecture appreciate on recommend" > "recession.txt"

208: Create a directory "discover" and then delete it.
mkdir discover && rmdir discover

209: Create a directory "interference" and then delete it.
mkdir interference && rmdir interference

210: Open a file in the terminal and see the content.
cat recession.txt

211: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

212: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

213: Delete the last folder you created.
rm -rf salvation

214: Create a file "advice.txt" containing: "recommend recession recommend banish recommend". Then output the unique lines.
echo "recommend recession recommend banish recommend" > advice.txt && echo "recommend recession recommend banish recommend" | tr ' ' '\n' | sort -u

215: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

216: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

217: Delete the last file you created.
rm advice.txt

218: Write something in a file called "support.txt".
echo "something" > support.txt

219: Use: echo "needle rider speculate salvation salvation" > "identity.txt" to create or overwrite "identity.txt".
echo "needle rider speculate salvation salvation" > "identity.txt"

220: Create a directory "salvation" and then delete it.
mkdir salvation && rmdir salvation

221: Create a file called "needle.txt".
touch needle.txt

222: Write something in a file called "speculate.txt".
echo "something" > speculate.txt

223: Delete the last file you created.
rm speculate.txt

224: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

225: Output the content of any file in the directory.
cat support.txt

226: Create a file called "relative.txt" and immediately delete it.
touch relative.txt && rm relative.txt

227: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

228: Move two folders up (but never leave "cli_sandbox").
cd ~/cli_sandbox

229: Create a file "appreciate.txt" containing: "regret stock regret ethics regret". Then output the sorted lines.
echo "regret stock regret ethics regret" > appreciate.txt && echo "regret stock regret ethics regret" | tr ' ' '\n' | sort

230: Output the full path you are on.
pwd

231: Delete the last folder you created.
find . -maxdepth 1 -type d -name "cli_sandbox" -exec rmdir {} \;

232: Create a file "advice.txt" containing: "degree nerve degree bay degree". Then output the unique lines.
echo "degree nerve degree bay degree" > advice.txt && echo "degree nerve degree bay degree" | tr ' ' '\n' | sort -u

233: Create a directory "terminal" and then delete it.
mkdir terminal && rmdir terminal

234: If already inside "cli_sandbox", create a directory "difficulty" and move into the directory.
mkdir difficulty && cd difficulty

235: Open a file in the terminal and see the content.
cd ~/cli_sandbox && cat advice.txt

236: Create a file called "depression.txt" and immediately delete it.
touch depression.txt && rm depression.txt

237: Create a file called "identity.txt" and immediately delete it.
rm identity.txt

238: Create two directories "bay" and "responsibility" and move into "responsibility".
mkdir bay responsibility && cd responsibility

239: Change directory to "cli_sandbox".
cd ~/cli_sandbox

240: Open a file in the terminal and see the content.
cat appreciate.txt

241: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

242: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

243: Write something in a file called "shallow.txt".
echo "something" > shallow.txt

244: Create a file called "panel.txt" and immediately delete it.
touch panel.txt && rm panel.txt

245: Write something in a file called "terminal.txt".
echo "something" > terminal.txt

246: Create a file called "beat.txt".
touch beat.txt

247: Create a file "appreciate.txt" containing: "beat surprise beat pray beat". Then output the sorted lines.
echo "beat surprise beat pray beat" > appreciate.txt && echo "beat surprise beat pray beat" | tr ' ' '\n' | sort

248: Create two directories "experienced" and "surprise" and move into "surprise".
cd ~/cli_sandbox && mkdir experienced surprise && cd surprise

249: Output the sizes of the files in the directory.
ls -lh

250: Create a file "recession.txt" containing: "recession ethics speculate pray pray". Then search recursively for "recession".
cd ~/cli_sandbox && echo "recession ethics speculate pray pray" > recession.txt && grep -r "recession" .

251: Change directory to "cli_sandbox".
cd ~/cli_sandbox

252: Create a file called "identity.txt".
touch identity.txt

253: Create a file called "surprise.txt" containing these words (one per line): "plane ethics plane support plane". Then count the lines.
echo -e "plane\nethics\nplane\nsupport\nplane" > surprise.txt && wc -l surprise.txt

254: Create a file "support.txt" containing: "depression honor depression pen depression". Then output the unique lines.
echo "depression honor depression pen depression" > support.txt && echo "depression honor depression pen depression" | tr ' ' '\n' | sort -u

255: Create a file "recommend.txt" containing: "panel architecture panel pray panel". Then output the sorted lines.
echo "panel architecture panel pray panel" > recommend.txt && echo "panel architecture panel pray panel" | tr ' ' '\n' | sort

256: Output hidden files.
ls -la

257: Output the content of any file in the directory.
cat recommend.txt

258: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

259: Use: echo "ditch advice experienced architecture stock" > "rain.txt" to create or overwrite "rain.txt".
echo "ditch advice experienced architecture stock" > "rain.txt"

260: If already inside "cli_sandbox", create a directory "depression" and move into the directory.
mkdir depression && cd depression

261: Output who the active user is.
whoami

262: Change directory to "cli_sandbox".
cd ~/cli_sandbox

263: Create a file called "speculate.txt" containing these words (one per line): "pioneer excuse pioneer cousin pioneer". Then count the lines.
echo -e "pioneer\nexcuse\npioneer\ncousin\npioneer" > speculate.txt && wc -l speculate.txt

264: Create a file called "evaluate.txt" and immediately delete it.
touch evaluate.txt && rm evaluate.txt

265: Output the content of any file in the directory.
cat speculate.txt

266: Open a file in the terminal and see the content.
cat recommend.txt

267: Use: echo "support pioneer beat identity proportion" > "responsibility.txt" to create or overwrite "responsibility.txt".
echo "support pioneer beat identity proportion" > "responsibility.txt"

268: Create a file called "nerve.txt".
touch nerve.txt

269: Open a file in the terminal and see the content.
cat responsibility.txt

270: Create a file "honor.txt" containing: "honor rain recession on discover". Then search recursively for "honor".
echo "honor rain recession on discover" > honor.txt && grep -r "honor" .

271: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

272: Open a file in the terminal and see the content.
cat honor.txt

273: Open a file in the terminal and see the content.
cat speculate.txt

274: Create a file called "sister.txt" and immediately delete it.
touch sister.txt && rm sister.txt

275: Open a file in the terminal and see the content.
cat responsibility.txt

276: Create a folder named "cli_sandbox".
mkdir -p cli_sandbox

277: Create a file called "panel.txt" and immediately delete it.
touch panel.txt && rm panel.txt

278: Open a file in the terminal and see the content.
cat support.txt

279: Delete the last file you created.
rm honor.txt

280: Delete the last file you created.
rm responsibility.txt

281: Delete the last file you created.
rm speculate.txt

282: Create a file called "speculate.txt".
touch speculate.txt

283: Create a file called "ditch.txt" containing these words (one per line): "degree cater degree beat degree". Then count the lines.
echo -e "degree\ncater\ndegree\nbeat\ndegree" > ditch.txt && wc -l ditch.txt

284: Change directory to "cli_sandbox".
cd ~/cli_sandbox

285: Create a file called "pioneer.txt" and immediately delete it.
touch pioneer.txt && rm pioneer.txt

286: Create a file "on.txt" containing: "surprise evaluate surprise panel surprise". Then output the unique lines.
echo "surprise evaluate surprise panel surprise" > on.txt && echo "surprise evaluate surprise panel surprise" | tr ' ' '\n' | sort -u

287: Delete the last folder you created.
rmdir depression

288: Create a directory "rain" and then delete it.
mkdir rain && rmdir rain

289: Move one level up (stay inside "cli_sandbox").
cd ~/cli_sandbox

290: Create a file called "sister.txt".
touch sister.txt

291: Create two directories "bay" and "proportion" and move into "proportion".
mkdir bay proportion && cd proportion

292: Change directory to "cli_sandbox".
cd ~/cli_sandbox

293: Output the sizes of the files in the directory.
ls -lh

294: Output the sizes of the files in the directory.
ls -lh

295: Output hidden files.
ls -la

296: Change directory to "cli_sandbox".
cd ~/cli_sandbox

297: Output what's in the directory.
ls

298: Create a file "evaluate.txt" containing: "sister rider sister honor sister". Then output the sorted lines.
echo "sister rider sister honor sister" > evaluate.txt && echo "sister rider sister honor sister" | tr ' ' '\n' | sort

299: Create a file "terminal.txt" containing: "evaluate stock evaluate pen evaluate". Then output the unique lines.
echo "evaluate stock evaluate pen evaluate" > terminal.txt && echo "evaluate stock evaluate pen evaluate" | tr ' ' '\n' | sort -u

300: Use: echo "beat salvation shallow speculate regret" > "ethics.txt" to create or overwrite "ethics.txt".
echo "beat salvation shallow speculate regret" > "ethics.txt"

301: If there are any directories in the current folder, then move inside any one of them.
cd $(ls -d */ | head -1)

302: Create a file called "plane.txt" and immediately delete it.
touch plane.txt && rm plane.txt

303: Output the full path you are on.
pwd

304: Delete the last folder you created.
cd ~/cli_sandbox && rmdir proportion

305: Write something in a file called "hill.txt".
echo "something" > hill.txt

306: Create a file called "interference.txt" and immediately delete it.
touch interference.txt && rm interference.txt

307: Create a file "needle.txt" containing: "support difficulty needle ethics advice". Then search recursively for "needle".
echo "support difficulty needle ethics advice" > needle.txt && grep -r "needle" .

308: Write something in a file called "cater.txt".
echo "something" > cater.txt

309: Output the sizes of the files in the directory.
ls -lh

310: Create a file called "sister.txt" and immediately delete it.
rm sister.txt && touch sister.txt && rm sister.txt

311: Create a file "proportion.txt" containing: "advice banish advantage stock proportion". Then search recursively for "proportion".
echo "advice banish advantage stock proportion" > proportion.txt && grep -r "proportion" .

312: Create a file called "panel.txt" and immediately delete it.
touch panel.txt && rm panel.txt

313: Output what's in the directory.
ls

314: Create two directories "ethics" and "cousin" and move into "cousin".
mkdir ethics cousin && cd cousin

315: Create a file "support.txt" containing: "interference advantage interference rider interference". Then output the unique lines.
cd ~/cli_sandbox && echo "interference advantage interference rider interference" > support.txt && echo "interference advantage interference rider interference" | tr ' ' '\n' | sort -u

316: Create a file "medal.txt" containing: "nerve correspondence nerve shot nerve". Then output the unique lines.
echo "nerve correspondence nerve shot nerve" > medal.txt && echo "nerve correspondence nerve shot nerve" | tr ' ' '\n' | sort -u

317: Output who the active user is.
whoami

318: Output the content of any file in the directory.
cat medal.txt

319: Output hidden files.
ls -la

320: If already inside "cli_sandbox", create a directory "nerve" and move into the directory.
mkdir nerve && cd nerve

321: Output who the active user is.
whoami

322: Create a file "hill.txt" containing: "difficulty surprise difficulty rain difficulty". Then output the unique lines.
echo "difficulty surprise difficulty rain difficulty" > hill.txt && echo "difficulty surprise difficulty rain difficulty" | tr ' ' '\n' | sort -u

323: Create a file called "needle.txt" containing these words (one per line): "recommend beat recommend relative recommend". Then count the lines.
echo -e "recommend\nbeat\nrecommend\nrelative\nrecommend" > needle.txt && wc -l needle.txt

324: Output the sizes of the files in the directory.
ls -lh

325: Create a file called "responsibility.txt".
touch responsibility.txt

326: Open a file in the terminal and see the content.
cat hill.txt

327: Delete the last folder you created.
cd ~/cli_sandbox && rm -rf nerve

328: Output hidden files.
ls -la

329: Create two directories "discover" and "advice" and move into "advice".
mkdir discover advice && cd advice

330: If already inside "cli_sandbox", create a directory "rider" and move into the directory.
cd ~/cli_sandbox && mkdir rider && cd rider

331: Create a folder named "cli_sandbox".
mkdir cli_sandbox

332: Open a file in the terminal and see the content.
cd ~/cli_sandbox && cat medal.txt

333: Open a file in the terminal and see the content.
cat support.txt

334: Output what's in the directory.
ls

335: Delete the last file you created.
rm support.txt

336: If already inside "cli_sandbox", create a directory "evaluate" and move into the directory.
mkdir evaluate && cd evaluate

337: If there are any directories in the current folder, then move inside any one of them.
cd ~/cli_sandbox && cd $(ls -d */ | head -1)

338: If already inside "cli_sandbox", create a directory "needle" and move into the directory.
cd ~/cli_sandbox && mkdir needle && cd needle

339: Change directory to "cli_sandbox".
cd ~/cli_sandbox

340: Create a directory "degree" and then delete it.
mkdir degree && rmdir degree

341: Create a file "advantage.txt" containing: "beat recommend beat sister beat". Then output the unique lines.
echo "beat recommend beat sister beat" > advantage.txt && echo "beat recommend beat sister beat" | tr ' ' '\n' | sort -u

342: Create a file called "surprise.txt".
touch surprise.txt

343: Create a file "plane.txt" containing: "bay honor bay recession bay". Then output the unique lines.
echo "bay honor bay recession bay" > plane.txt && echo "bay honor bay recession bay" | tr ' ' '\n' | sort -u

344: Delete the last folder you created.
rmdir needle

345: Output hidden files.
ls -la

346: Output what's in the directory.
ls

347: Open a file in the terminal and see the content.
cat advantage.txt

348: Use: echo "discover support architecture recession excuse" > "salvation.txt" to create or overwrite "salvation.txt".
echo "discover support architecture recession excuse" > "salvation.txt"

349: Create a file "depression.txt" containing: "medal panel medal identity medal". Then output the unique lines.
echo "medal panel medal identity medal" > depression.txt && echo "medal panel medal identity medal" | tr ' ' '\n' | sort -u

350: Delete the last file you created.
rm depression.txt